#include <stdio.h>

int main(){

    printf("Integer'ın kapladığı yer:%d",sizeof(int));
    printf("\n\nchar'ın kapladığı yer:%d",sizeof(char));
    printf("\n\nFloat'ın kapladığı yer:%d",sizeof(float));
		printf(\n\nDouble'ın kapladığı yer:%d,sizeof(double));
		
   
   return 0;
   
}
